// Nhập đối tượng api đã được cấu hình sẵn để gửi các yêu cầu HTTP (Axios)
import { api } from "../lib/api";

// Cần triển khai các gọi hàm API cho refereeService
// Ánh xạ 1-1 với các endpoint tương ứng trong Java Controller của Trọng tài (Referee)
export const refereeService = {};
